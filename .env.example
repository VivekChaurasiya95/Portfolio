# Frontend environment variables
# Copy this file to .env and fill in your values

# URL of the deployed backend API (Render service URL)
# Leave empty to use the Vite dev server proxy (localhost development)
VITE_API_URL=http://localhost:5000
